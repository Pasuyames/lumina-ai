// Vitest ortamında "server-only" paketi hata fırlatır (yalnızca Next.js
// derleme zamanı içindir). Test ortamında zararsız bir stub'a yönlendirilir
// — bkz. vitest.config.ts → resolve.alias.
export {};
